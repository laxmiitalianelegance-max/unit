  text-align: end;
  overflow-wrap: anywhere;
  white-space: pre-wrap;
}

.pws-warn {
  margin-top: 10px;
  padding: 10px;
  border: 1px solid #604b2d;
  border-radius: 9px;
  background: #302617;
  color: #f7ca7f;
  font-size: 11px;
}

.u369-product-form {
  padding: 17px;
}

.u369-form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14px;
}

.u369-field {
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 6px;
  color: var(--u-muted);
  font-size: 11px;
  font-weight: 600;
}

.u369-field-wide {
  grid-column: 1 / -1;
}

.u369-field input,
.u369-field textarea,
.u369-field select,
.ss-input,
.ss-select {
  width: 100%;
  min-width: 0;
  border: 1px solid var(--u-border);
  border-radius: 10px;
  background: #1f1f1f;
  color: var(--u-text);
}

.u369-field input,
.u369-field select,
.ss-input,
.ss-select {
  min-height: 42px;
  padding: 9px 11px;
}

.u369-field textarea {
  min-height: 118px;
  padding: 10px 11px;
  resize: vertical;
  line-height: 1.5;
}

.u369-field input::placeholder,
.u369-field textarea::placeholder,
.ss-input::placeholder {
  color: #707070;
}

.u369-field input:focus,
.u369-field textarea:focus,
.u369-field select:focus,
.ss-input:focus,
.ss-select:focus {
  border-color: #6d6d6d;
  outline: none;
  box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.06);
}

.u369-field input[type="file"] {
  padding: 8px;
  color: var(--u-muted);
  font-size: 11px;
}

.u369-field input[type="file"]::file-selector-button {
  margin-inline-end: 9px;
  padding: 6px 9px;
  border: 0;
  border-radius: 7px;
  background: #363636;
  color: #e7e7e7;
  font: inherit;
}

.u369-recent-products {
  margin-top: 17px;
  padding: 16px;
}

.u369-recent-products h2 {
  margin: 0 0 12px;
  font-size: 15px;
}

.u369-products-list {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 10px;
}

.u369-product-card {
  min-width: 0;
  display: grid;
  grid-template-columns: 52px minmax(0, 1fr);
  gap: 11px;
  padding: 10px;
  border: 1px solid var(--u-border-soft);
  border-radius: 11px;
  background: #202020;
}

.u369-product-thumb {
  width: 52px;
  height: 52px;
  display: grid;
  place-items: center;
  overflow: hidden;
  border-radius: 9px;
  background: #303030;
  color: var(--u-faint);
  font-size: 10px;
}

.u369-product-thumb img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.u369-product-info {
  min-width: 0;
}

.u369-product-name {
  overflow: hidden;
  color: #ebebeb;
  font-size: 12px;
  font-weight: 650;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.u369-product-meta {
  margin-top: 4px;
  color: var(--u-faint);
  font-size: 10px;
  line-height: 1.4;
}

.u369-empty-list,
.u369-list-error {
  grid-column: 1 / -1;
  padding: 12px;
  border: 1px dashed var(--u-border);
  border-radius: 10px;
  color: var(--u-muted);
  font-size: 11px;
  text-align: center;
}

.u369-list-error {
  color: #ff9aa7;
}

.u369-settings-container {
  max-width: 760px;
}

.ss-card {
  margin-top: 14px;
  padding: 16px;
}

.ss-title {
  margin: 0 0 12px;
  font-size: 14px;
  font-weight: 700;
}

.ss-card-head {
  justify-content: space-between;
  margin-bottom: 12px;
}

.ss-card-head .ss-title {
  margin: 0;
}

.ss-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 9px;
  margin-bottom: 11px;
}

.ss-item {
  min-width: 0;
  padding: 11px;
  border: 1px solid var(--u-border-soft);
  border-radius: 10px;
  background: #202020;
}

.ss-item-row {
  display: flex;
  align-items: center;
  gap: 8px;
}

.ss-name {
  min-width: 0;
  flex: 1;
  overflow: hidden;
  font-size: 12px;
  font-weight: 650;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.ss-dot {
  width: 8px;
  height: 8px;
  flex: none;
  border-radius: 50%;
  background: var(--u-danger);
}

.ss-dot.ok {
  background: var(--u-success);
}

.ss-meta {
  margin-top: 5px;
  color: var(--u-faint);
  font-size: 10px;
}

.ss-field {
  margin-top: 12px;
}

.ss-toggle,
.ss-row-static {
  min-height: 46px;
  justify-content: space-between;
  border-top: 1px solid var(--u-border-soft);
  color: #dddddd;
  font-size: 12px;
}

.ss-toggle {
  display: flex;
  align-items: center;
  gap: 12px;
  cursor: pointer;
}

.ss-check {
  width: 20px;
  height: 20px;
  accent-color: var(--u-accent);
}

#u369-lang-custom {
  margin-top: 9px;
}

#ss-reset {
  margin-top: 12px;
}

body.compact {
  --u-sidebar-width: 238px;
  --u-content-width: 740px;
}

body.compact .u369-turn {
  margin-bottom: 25px;
}

body.compact .u369-conversation {
  padding-top: 18px;
}

html[dir="rtl"] .u369-sidebar {
  left: auto;
  right: 0;
  border-inline-start: 1px solid #252525;
  border-inline-end: 0;
}

html[dir="rtl"] .u369-topbar {
  left: 0;
  right: var(--u-sidebar-width);
}

html[dir="rtl"] .u369-main {
  margin-inline-start: var(--u-sidebar-width);
}

html[dir="rtl"] .u369-chat-composer {
  padding: 8px 15px 8px 56px;
}

html[dir="rtl"] .u369-chat-send {
  right: auto;
  left: 9px;
}

html[dir="rtl"] input[type="email"],
html[dir="rtl"] input[type="url"],
html[dir="rtl"] input[name="sku"],
html[dir="rtl"] input[inputmode="decimal"] {
  direction: ltr;
  text-align: left;
}

html.locale-long .u369-side-item,
html.locale-long .u369-mode-btn {
  font-size: 11.5px;
}

@media (max-width: 820px) {
  :root,
  body.compact {
    --u-sidebar-width: min(86vw, 310px);
  }

  .u369-sidebar {
    transform: translateX(-103%);
    transition: transform 180ms ease;
    box-shadow: 18px 0 50px rgba(0, 0, 0, 0.38);
  }

  html[dir="rtl"] .u369-sidebar {
    transform: translateX(103%);
    box-shadow: -18px 0 50px rgba(0, 0, 0, 0.38);
  }

  .u369-sidebar.open {
    transform: translateX(0) !important;
  }

  .u369-drawer-shade {
    display: block;
    pointer-events: none;
  }

  .u369-drawer-shade.open {
    pointer-events: auto;
  }

  .u369-topbar,
  html[dir="rtl"] .u369-topbar {
    left: 0;
    right: 0;
    padding: 0 10px;
    border-bottom-color: #2b2b2b;
  }

  .u369-menu {
    display: grid;
  }

  .u369-main,
  html[dir="rtl"] .u369-main {
    margin-inline-start: 0;
  }

  .u369-offline {
    left: 12px;
    right: 12px;
  }

  .u369-conversation {
    padding: 20px 14px 20px;
  }

  .u369-user-msg {
    max-width: 89%;
  }

  .u369-composer-zone {
    padding-inline: 10px;
  }

  .u369-page-container {
    padding: 24px 14px 70px;
  }

  .pws-checks {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }
}

@media (max-width: 560px) {
  .u369-work-sub {
    display: none;
  }

  .u369-form-grid,
  .u369-products-list,
  .ss-grid {
    grid-template-columns: 1fr;
  }

  .u369-field-wide {
    grid-column: auto;
  }

  .pws-head {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .pws-checks {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .u369-page-head {
    align-items: flex-start;
  }

  .pws-row {
    align-items: flex-start;
    flex-direction: column;
    gap: 3px;
  }

  .pws-row strong {
    max-width: 100%;
    text-align: start;
  }
}

@media (max-width: 360px) {
  .u369-chat-composer {
    padding-inline-start: 13px;
  }

  .u369-empty-title {
    font-size: 21px;
  }

  .pws-checks {
    grid-template-columns: 1fr;
  }
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    scroll-behavior: auto !important;
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
